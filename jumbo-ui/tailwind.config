/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
    "./public/index.html",
  ],
  theme: {
    extend: {
      animation: {
        'soundwave-pulse': 'soundwave-pulse 1.5s ease-out infinite',
        'particle-float': 'particle-float 3s ease-in-out infinite',
        'glow-pulse': 'glow-pulse 2s ease-in-out infinite',
        'subtle-tilt': 'subtle-tilt 3s ease-in-out infinite',
        'zoom-in': 'zoom-in 0.5s ease-out forwards',
        'bounce': 'bounce 1.4s ease-in-out infinite',
      },
      keyframes: {
        'soundwave-pulse': {
          '0%': { r: '0', opacity: '0.6' },
          '100%': { r: '100', opacity: '0' },
        },
        'particle-float': {
          '0%, 100%': { transform: 'translate(0, 0) scale(1)', opacity: '0.6' },
          '50%': { transform: 'translate(var(--tx), var(--ty)) scale(1.2)', opacity: '1' },
        },
        'glow-pulse': {
          '0%, 100%': { filter: 'drop-shadow(0 0 16px rgba(78, 205, 196, 0.3))' },
          '50%': { filter: 'drop-shadow(0 0 24px rgba(78, 205, 196, 0.6))' },
        },
        'subtle-tilt': {
          '0%, 100%': { transform: 'perspective(1000px) rotateX(0deg) rotateY(0deg) scale(1)' },
          '50%': { transform: 'perspective(1000px) rotateX(0deg) rotateY(2deg) scale(1.01)' },
        },
        'zoom-in': {
          'from': { transform: 'scale(1)' },
          'to': { transform: 'scale(1.05)' },
        },
        'bounce': {
          '0%, 80%, 100%': { opacity: '0.5', transform: 'scale(0.8)' },
          '40%': { opacity: '1', transform: 'scale(1)' },
        },
      },
    },
  },
  plugins: [],
}